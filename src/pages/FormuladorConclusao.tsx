// Rota /formulador/conclusao — tela de revisão.
// Placeholder — o conteúdo final (resumo + banner + botões) é implementado no passo 7 do plano.

import Card from '@/components/ui/Card'

export default function FormuladorConclusao() {
  return (
    <Card padding="lg" radius="md">
      <p className="typo-body">Tela de conclusão em construção.</p>
    </Card>
  )
}
