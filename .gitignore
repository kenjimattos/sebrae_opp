# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files — use .env.example como referência
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Norfolk Narrow é licenciada só para uso pessoal: o arquivo fica na máquina de
# quem desenha, nunca no repo nem no bundle. Ela existe aqui só como fonte de
# onde o contorno do logotipo foi extraído (ver PippaWordmark.tsx).
src/assets/fonts/Norfolk*

# Claude
CLAUDE.md
.claude/
