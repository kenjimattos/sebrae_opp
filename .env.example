# Microsoft Clarity — ID do projeto obtido em https://clarity.microsoft.com
# Deixe em branco pra desabilitar Clarity (dev / previews).
# Só é lido em build de produção (import.meta.env.PROD).
VITE_CLARITY_ID=
